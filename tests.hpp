#ifndef TESTS_HPP
#define TESTS_HPP
#include <iostream>
#include <string>
#include <vector>

using namespace std;

class Test {
public:
    void tests();
    void compare(vector<string> list_hash, vector<string> list);
};

#endif // TESTS_HPP
