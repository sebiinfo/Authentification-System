#ifndef TESTS_HPP
#define TESTS_HPP
#include <iostream>
#include <string>
#include <vector>

void tests();
void compare(std::vector<std::string> list_hash, std::vector<std::string> list);

#endif // TESTS_HPP
