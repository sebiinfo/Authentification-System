#ifndef MAIN_H
#define MAIN_H
#include <iostream>
#include <string>
#include <vector>

void tests();
void compare(std::vector<std::string> list_hash, std::vector<std::string> list);

#endif // MAIN_H
